apiVersion: v1
kind: Service
metadata:
  name: el-github-event-listener
  labels:
    app.kubernetes.io/name: github-event-listener
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app.kubernetes.io/name: github-event-listener
